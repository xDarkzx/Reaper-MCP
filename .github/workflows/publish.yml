name: Publish to PyPI

# Fires automatically on every version tag push (git tag vX.Y.Z && git push
# --tags), or manually via workflow_dispatch — no manual `twine upload` step,
# and no PyPI token stored anywhere in this repo. Uses PyPI's Trusted
# Publisher (OIDC) mechanism instead: PyPI is configured (once, on pypi.org)
# to trust this exact workflow in this exact repo, so GitHub can prove its
# identity to PyPI without a shared secret to store, rotate, or leak.
#
# Package name on PyPI is "xdarkzx-reaper-mcp" — "reaper-mcp" was already
# taken by an unrelated project, and PyPI's anti-typosquatting check
# rejected the plain "reapermcp" variant as too similar to it. The console
# command stays "reaper-mcp" either way ([project.scripts] in
# pyproject.toml), so install docs / MCP client configs referencing that
# command are unaffected.
"on":
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  test:
    # Gate: publishing requires the tagged commit itself to pass tests, not
    # just whatever main's CI run said earlier — a tag can point anywhere.
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -e ".[dev,analysis]"
      - run: pytest tests/ -x -q

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade build
      - run: python -m build
      - uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # required for Trusted Publishing — do not remove
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
